services:
  crm-app:
    build: .
    container_name: crm-demo
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - JWT_SECRET=troque-este-segredo-em-producao
    volumes:
      - crm-data:/app/data
    restart: unless-stopped

volumes:
  crm-data:
